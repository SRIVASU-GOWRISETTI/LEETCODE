class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* prev ,*curr ,*forward;
        prev=NULL;
        curr=head;
        while(curr!=NULL){
             forward=curr->next;
            curr->next=prev;
            prev=curr;
            curr=forward;
        }
     return prev;
        }
};
